import { colors } from "./colors";
import { size, lineHeights } from "./spacing";
import { fonts, fontWeights, fontSizes } from "./fonts";

export const theme = {
  colors,
  size,
  lineHeights,
  fonts,
  fontSizes,
  fontWeights,
};